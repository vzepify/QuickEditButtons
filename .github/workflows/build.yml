name: Build Android Geode Mod

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y cmake ninja-build unzip wget

      - name: Download Android NDK
        run: |
          wget -q https://dl.google.com/android/repository/android-ndk-r26b-linux.zip
          unzip -q android-ndk-r26b-linux.zip
          echo "ANDROID_NDK_HOME=$PWD/android-ndk-r26b" >> $GITHUB_ENV

      - name: Clone Geode SDK
        run: |
          git clone --depth 1 https://github.com/geode-sdk/geode.git geode-sdk
          echo "GEODE_SDK=$PWD/geode-sdk" >> $GITHUB_ENV

      - name: Install Geode CLI
        run: |
          wget -q https://github.com/geode-sdk/cli/releases/latest/download/geode-cli-linux.zip || \
          wget -q $(curl -s https://api.github.com/repos/geode-sdk/cli/releases/latest | grep browser_download_url | grep linux | head -1 | cut -d'"' -f4)
          unzip -q *.zip
          sudo mv geode /usr/local/bin/geode || true
          sudo chmod +x /usr/local/bin/geode

      - name: Configure CMake
        run: |
          cmake -B build-android \
            -DCMAKE_BUILD_TYPE=Release \
            -DGEODE_TARGET_PLATFORM=Android \
            -DANDROID_ABI=arm64-v8a \
            -DANDROID_PLATFORM=android-23 \
            -DCMAKE_TOOLCHAIN_FILE=$ANDROID_NDK_HOME/build/cmake/android.toolchain.cmake \
            -G Ninja

      - name: Build
        run: cmake --build build-android --config Release

      - name: Upload .geode file
        uses: actions/upload-artifact@v4
        with:
          name: QuickEditButtons
          path: build-android/QuickEditButtons.geode
          if-no-files-found: warn
